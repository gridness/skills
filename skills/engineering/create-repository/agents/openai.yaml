interface:
  display_name: "Create repository"
  short_description: "Scaffold a typed repository with optional GitHub setup"
  default_prompt: "Use $create-repository to scaffold a Rust repository and optionally configure it on GitHub."
policy:
  allow_implicit_invocation: false
